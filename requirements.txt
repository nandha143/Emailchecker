# Smart Email Generator & Formatter Requirements
# Core Dependencies

# Groq API client for LLM interactions
groq==0.4.1

# Flask web framework for backend API
Flask==2.3.3

# Flask CORS extension for cross-origin requests
Flask-CORS==4.0.0

# Additional Flask dependencies (automatically installed with Flask)
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7

# HTTP client library (dependency of groq)
httpx==0.24.1

# JSON handling (built-in, but listing for completeness)
# json - built-in Python module

# Regular expressions (built-in)
# re - built-in Python module

# Operating system interface (built-in)
# os - built-in Python module

# Date and time handling (built-in)
# datetime - built-in Python module

# Optional: For enhanced development experience
python-dotenv==1.0.0  # For loading environment variables from .env file

# Optional: For production deployment
gunicorn==21.2.0  # WSGI HTTP Server for UNIX

# Optional: For API documentation (if you want to add Swagger)
# flask-restx==1.2.0

# Optional: For enhanced logging
# structlog==23.1.0

# Development dependencies (optional)
# pytest==7.4.2  # For testing
# black==23.7.0   # Code formatter
# flake8==6.0.0   # Linter